query GetFileList($limit: Int) {
  tickers(limit: $limit) {
    id
    symbol
    name
    assetClass
    exchange
  }

  strategies {
    id
    name
    type
    description
    createdAt
    updatedAt
  }
}
