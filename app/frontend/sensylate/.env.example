# API Configuration
# Set to 'true' to use GraphQL API instead of REST
VITE_USE_GRAPHQL=false

# GraphQL endpoint (if different from default)
# VITE_GRAPHQL_ENDPOINT=http://localhost:8000/graphql

# Enable GraphQL development tools
VITE_ENABLE_GRAPHQL_DEVTOOLS=true
